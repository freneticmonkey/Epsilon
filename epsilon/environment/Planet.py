
class Planet:
    def __init__(self, seed, orbitDist, radius, gravity, hasOrbitors):
        self._seed = seed
        self._orbitDistance = orbitDist
        self._radius = radius
        self._gravity = gravity
        self._hasOrbitors = hasOrbitors
        
    def _generatePlanet(self):
        #Generate planet stuff here
        
        #Generate orbiting bodies
        pass
        